{
  "name": "alpha-quarante",
  "version": "1.0.0",
  "description": "Site Alpha Quarante — inscription + email",
  "private": true,
  "scripts": {
    "start": "node backend/server.js",
    "install:backend": "npm install --prefix backend"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
